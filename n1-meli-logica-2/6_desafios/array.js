//let max = Math.max(array.length);
//pesquisar sobre infitiny
/*6 - Crie um programa que receba o array = [5, 37, 18, 59, 12, -5] e faça as seguintes operações:
    - Printe o maior;
    - Printe o menor.*/

const lista = [5, 37, 18, 58, 12, -5];
const maior = 0;
const menos = 0;



for (let i = 0; i < arry.length; i++){
    if(array[i] > maior){
        maior = array[i];
        //console.log()
    }
    if (array[i] < menor){
        menor = array[i];
    }

    console.log(`Maior ${maior}, ${menor}`);

}